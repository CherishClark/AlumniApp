<div class="container">
  <div class="card">
    <p id="notice"><%= notice %></p>

    <p>
      <h4>Ranking: <%= @post.ranking %></h4>
    </p>

    <h4>Topic:</h4>
    <p>
      <%= @post.topic %>
    </p>

    <h4>Post:</h4>
    <p>
      <%= markdown @post.body %>
    </p>

    <h4>Comment:</h4>
    <p>
      <%= markdown @post.comment %>
    </p>

    <p>
      <%= @post.images %>
    </p>
  </div>


<%= render 'comments/comment_form' %>



<<<<<<< HEAD
<%= form_with(model: @post, local: true) do |form| %>

  <div class="form-group">
    <%= form.text_area :comment, id: :post_comment, class: 'form-control', rows: 7, placeholder: 'Your Comment
```language
    Your Code Here
``` 
(Feel free to use markdown for code snippets.  Default language set to Ruby)' %>
  </div>

   <div class="form-group">
    <%= form.submit "Post Comment", class: 'btn btn-primary' %>
  </div>
<% end %>

<!-- comment form -->


<!-- comment form -->

<!-- comment form -->

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
</div>


<div id="comments" data-post-id="<%= @post.id %>">
  <%= render @post.comments %>
</div>

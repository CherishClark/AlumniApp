<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.number_field :ranking, id: :post_ranking, class: 'form-control', placeholder: 'Post Ranking' %>
  </div>

  <div class="form-group">
    <%= form.text_field :topic, id: :post_topic, class: 'form-control', placeholder: 'Post Topic' %>
  </div>

  <div class="form-group">
    <%= form.text_area :body, id: :post_body class: 'form-control', rows: 15, placeholder: '```
    Your Code Here
``` 
(Include three back ticks before and after your code as shown here to save it with the correct syntax.  Back ticks are on the button between tab and esc on a regular keyboard)' %>
  </div>

  <div class="form-group">
    <%= form.label :images %>
    <%= form.text_area :images, id: :post_images %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: 'btn btn-secondary' %>
  </div>
<% end %>
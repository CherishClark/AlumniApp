<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ranking %>
    <%= form.number_field :ranking, id: :post_ranking %>
  </div>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic, id: :post_topic %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :post_body %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :post_comment %>
  </div>

  <div class="field">
    <%= form.label :images %>
    <%= form.text_area :images, id: :post_images %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
